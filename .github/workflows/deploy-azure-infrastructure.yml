name: Deploy Azure Infrastructure (OIDC)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod
      environment_tag:
        description: 'Environment Tag (Format: SIT-USERID-TEAMID-YYYYMMDD-HHMM) - Used for resource isolation and state management. Example: SIT-john-teamA-20251120-1430'
        required: true
        type: string
      location:
        description: 'Azure Region'
        required: true
        default: 'uksouth'
        type: string

env:
  TF_VERSION: '1.6.0'
  AZURE_LOCATION: ${{ github.event.inputs.location || 'uksouth' }}
  ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}
  ENVIRONMENT_TAG: ${{ github.event.inputs.environment_tag }}

jobs:
  setup-backend:
    name: Setup Terraform Backend
    runs-on: ubuntu-latest
    outputs:
      backend_resource_group: ${{ steps.backend.outputs.backend_resource_group }}
      backend_storage_account: ${{ steps.backend.outputs.backend_storage_account }}
    permissions:
      id-token: write   # Required for OIDC
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login using OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set Azure Subscription
        run: |
          az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          az account show

      - name: Setup Terraform backend
        id: backend
        working-directory: Azure
        run: |
          chmod +x scripts/setup-terraform-backend.sh
          export AZURE_LOCATION=${{ env.AZURE_LOCATION }}
          export PROJECT_NAME=testcontainers
          
          # Run setup script and capture outputs
          SETUP_OUTPUT=$(./scripts/setup-terraform-backend.sh)
          echo "$SETUP_OUTPUT"
          
          # Extract and set outputs for other jobs
          BACKEND_RG=$(echo "$SETUP_OUTPUT" | grep "export TF_BACKEND_RESOURCE_GROUP" | cut -d'"' -f2)
          BACKEND_SA=$(echo "$SETUP_OUTPUT" | grep "export TF_BACKEND_STORAGE_ACCOUNT" | cut -d'"' -f2)
          
          echo "backend_resource_group=${BACKEND_RG}" >> $GITHUB_OUTPUT
          echo "backend_storage_account=${BACKEND_SA}" >> $GITHUB_OUTPUT

  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: setup-backend
    permissions:
      id-token: write       # Required for OIDC
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Azure Login using OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Create terraform.tfvars
        working-directory: Azure/terraform
        run: |
          cat > terraform.tfvars <<EOF
          location     = "${{ env.AZURE_LOCATION }}"
          project_name = "testcontainers"
          environment  = "${{ env.ENVIRONMENT }}"
          environment_tag = "${{ env.ENVIRONMENT_TAG }}"
          
          vnet_address_space    = ["10.0.0.0/16"]
          public_subnet_address_prefix   = ["10.0.1.0/24"]
          private_subnet_address_prefix  = ["10.0.2.0/24"]
          
          vm_size        = "Standard_D2s_v3"
          admin_username = "azureuser"
          
          github_repo_url      = "https://github.com/${{ github.repository }}"
          github_runner_name   = "azure-vm-runner-${{ env.ENVIRONMENT_TAG }}"
          github_runner_labels = ["self-hosted", "azure", "linux", "docker", "${{ env.ENVIRONMENT }}", "${{ env.ENVIRONMENT_TAG }}"]
          
          # OIDC Configuration
          github_org  = "${{ github.repository_owner }}"
          github_repo = "${{ github.event.repository.name }}"
          EOF

      - name: Update backend configuration
        working-directory: Azure/terraform
        run: |
          cat > backend.tf <<EOF
          terraform {
            backend "azurerm" {
              resource_group_name  = "${{ needs.setup-backend.outputs.backend_resource_group }}"
              storage_account_name = "${{ needs.setup-backend.outputs.backend_storage_account }}"
              container_name       = "tfstate"
              key                  = "azure/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate"
            }
          }
          EOF

      - name: Terraform Init
        working-directory: Azure/terraform
        run: terraform init
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_OIDC: true

      - name: Terraform Validate
        working-directory: Azure/terraform
        run: terraform validate

      - name: Terraform Format Check
        working-directory: Azure/terraform
        run: terraform fmt -check -recursive || true

      - name: Terraform Plan
        working-directory: Azure/terraform
        env:
          TF_VAR_github_pat: ${{ secrets.PAT_TOKEN }}
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_OIDC: true
        run: |
          terraform plan \
            -out=tfplan \
            -input=false

      - name: Upload plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}
          path: Azure/terraform/tfplan
          retention-days: 5

  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: [setup-backend, terraform-plan]
    permissions:
      id-token: write   # Required for OIDC
      contents: read
    environment:
      name: ${{ github.event.inputs.environment }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Azure Login using OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set Azure Subscription
        run: |
          az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          az account show

      - name: Create terraform.tfvars
        working-directory: Azure/terraform
        run: |
          cat > terraform.tfvars <<EOF
          location     = "${{ env.AZURE_LOCATION }}"
          project_name = "testcontainers"
          environment  = "${{ env.ENVIRONMENT }}"
          environment_tag = "${{ env.ENVIRONMENT_TAG }}"
          
          vnet_address_space    = ["10.0.0.0/16"]
          public_subnet_address_prefix   = ["10.0.1.0/24"]
          private_subnet_address_prefix  = ["10.0.2.0/24"]
          
          vm_size        = "Standard_D2s_v3"
          admin_username = "azureuser"
          
          github_repo_url      = "https://github.com/${{ github.repository }}"
          github_runner_name   = "azure-vm-runner-${{ env.ENVIRONMENT_TAG }}"
          github_runner_labels = ["self-hosted", "azure", "linux", "docker", "${{ env.ENVIRONMENT }}", "${{ env.ENVIRONMENT_TAG }}"]
          
          # OIDC Configuration
          github_org  = "${{ github.repository_owner }}"
          github_repo = "${{ github.event.repository.name }}"
          EOF

      - name: Update backend configuration
        working-directory: Azure/terraform
        run: |
          cat > backend.tf <<EOF
          terraform {
            backend "azurerm" {
              resource_group_name  = "${{ needs.setup-backend.outputs.backend_resource_group }}"
              storage_account_name = "${{ needs.setup-backend.outputs.backend_storage_account }}"
              container_name       = "tfstate"
              key                  = "azure/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate"
            }
          }
          EOF

      - name: Terraform Init
        working-directory: Azure/terraform
        run: terraform init
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_OIDC: true

      - name: Download plan
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}
          path: Azure/terraform/

      - name: Terraform Apply
        working-directory: Azure/terraform
        env:
          TF_VAR_github_pat: ${{ secrets.PAT_TOKEN }}
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_OIDC: true
        run: |
          terraform apply \
            -auto-approve \
            -input=false \
            tfplan

      - name: Terraform Output
        working-directory: Azure/terraform
        run: |
          echo "## Terraform Outputs" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          terraform output >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Save outputs to artifacts
        working-directory: Azure/terraform
        run: |
          terraform output -json > outputs.json

      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: terraform-outputs-${{ env.ENVIRONMENT }}
          path: Azure/terraform/outputs.json
          retention-days: 30

  rollback-on-failure:
    name: Rollback on Failure
    runs-on: ubuntu-latest
    needs: [setup-backend, terraform-plan, terraform-apply]
    if: failure()
    permissions:
      id-token: write
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login using OIDC
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set Azure Subscription
        run: |
          az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          az account show

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Check if infrastructure was partially deployed
        id: check-state
        working-directory: Azure/terraform
        continue-on-error: true
        run: |
          # Try to check if state file exists in Azure Storage
          if [ -n "${{ needs.setup-backend.outputs.backend_storage_account }}" ]; then
            BLOB_EXISTS=$(az storage blob exists \
              --account-name ${{ needs.setup-backend.outputs.backend_storage_account }} \
              --container-name tfstate \
              --name azure/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate \
              --auth-mode login \
              --query exists -o tsv)
            echo "state_exists=$BLOB_EXISTS" >> $GITHUB_OUTPUT
          else
            echo "state_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Destroy partially created infrastructure
        id: destroy
        if: steps.check-state.outputs.state_exists == 'true'
        working-directory: Azure/terraform
        continue-on-error: true
        env:
          TF_VAR_github_pat: ${{ secrets.PAT_TOKEN }}
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_USE_OIDC: true
        run: |
          cat > terraform.tfvars <<EOF
          location      = "${{ env.AZURE_LOCATION }}"
          project_name  = "testcontainers"
          environment   = "${{ env.ENVIRONMENT }}"
          environment_tag = "${{ env.ENVIRONMENT_TAG }}"
          
          vnet_address_space     = ["10.0.0.0/16"]
          public_subnet_prefix   = "10.0.1.0/24"
          private_subnet_prefix  = "10.0.2.0/24"
          
          vm_size = "Standard_D2s_v3"
          
          github_repo_url      = "https://github.com/${{ github.repository }}"
          github_runner_name   = "azure-vm-runner-${{ env.ENVIRONMENT_TAG }}"
          github_runner_labels = ["self-hosted", "azure", "linux", "docker", "${{ env.ENVIRONMENT }}", "${{ env.ENVIRONMENT_TAG }}"]
          
          github_org                     = "${{ github.repository_owner }}"
          github_repo                    = "${{ github.event.repository.name }}"
          terraform_state_resource_group = "${{ needs.setup-backend.outputs.backend_resource_group }}"
          terraform_state_storage_account = "${{ needs.setup-backend.outputs.backend_storage_account }}"
          EOF
          
          cat > backend.tf <<EOF
          terraform {
            backend "azurerm" {
              resource_group_name  = "${{ needs.setup-backend.outputs.backend_resource_group }}"
              storage_account_name = "${{ needs.setup-backend.outputs.backend_storage_account }}"
              container_name       = "tfstate"
              key                  = "azure/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate"
              use_oidc             = true
            }
          }
          EOF
          
          terraform init
          
          # Destroy all modules EXCEPT OIDC
          terraform destroy \
            -target=module.vm \
            -target=module.security \
            -target=module.networking \
            -target=azurerm_resource_group.main \
            -auto-approve
          
          # Capture exit code
          DESTROY_EXIT_CODE=$?
          echo "exit_code=$DESTROY_EXIT_CODE" >> $GITHUB_OUTPUT
          
          if [ $DESTROY_EXIT_CODE -eq 0 ]; then
            echo "success=true" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
          fi

      - name: Purge soft-deleted Key Vaults
        if: always()
        continue-on-error: true
        run: |
          echo "Checking for soft-deleted Key Vaults..."
          
          # List soft-deleted key vaults for this environment tag
          DELETED_KVS=$(az keyvault list-deleted \
            --query "[?tags.EnvironmentTag=='${{ env.ENVIRONMENT_TAG }}'].name" \
            -o tsv 2>/dev/null || echo "")
          
          if [ -z "$DELETED_KVS" ]; then
            echo "No soft-deleted Key Vaults found for environment tag: ${{ env.ENVIRONMENT_TAG }}"
            exit 0
          fi
          
          # Purge each soft-deleted key vault
          for KV_NAME in $DELETED_KVS; do
            echo "Purging Key Vault: $KV_NAME"
            az keyvault purge --name "$KV_NAME" --no-wait || true
          done
          
          echo "✅ Key Vault purge initiated"

      - name: Clean up Terraform state
        if: steps.destroy.outputs.success == 'true'
        continue-on-error: true
        run: |
          if [ -n "${{ needs.setup-backend.outputs.backend_storage_account }}" ]; then
            echo "Destroy succeeded, cleaning up state file..."
            az storage blob delete \
              --account-name ${{ needs.setup-backend.outputs.backend_storage_account }} \
              --container-name tfstate \
              --name azure/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate \
              --auth-mode login
            echo "✅ State file deleted"
          fi

      - name: State cleanup skipped
        if: steps.destroy.outputs.success != 'true'
        run: |
          echo "⚠️ Destroy failed or was skipped, preserving state file for manual cleanup" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Manual Cleanup Required" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To manually clean up:" >> $GITHUB_STEP_SUMMARY
          echo "1. Fix the destroy issue" >> $GITHUB_STEP_SUMMARY
          echo "2. Run: \`terraform destroy\`" >> $GITHUB_STEP_SUMMARY
          echo "3. Delete state file manually if needed" >> $GITHUB_STEP_SUMMARY

      - name: Rollback Summary
        if: always()
        run: |
          echo "## 🔄 Rollback Completed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The deployment failed and rollback was attempted." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Actions taken:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Checked for partially deployed infrastructure" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Destroyed any partially created resources" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Purged soft-deleted Key Vaults" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.destroy.outputs.success }}" == "true" ]; then
            echo "- ✅ Cleaned up Terraform state file" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ⚠️ State file preserved (destroy incomplete)" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Note:** Backend resources (Resource Group and Storage Account) are preserved for future deployments." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Please review the workflow logs to identify the failure cause." >> $GITHUB_STEP_SUMMARY
