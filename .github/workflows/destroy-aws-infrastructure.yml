name: Destroy AWS Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to destroy'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod
      aws_region:
        description: 'AWS Region'
        required: true
        default: 'eu-west-2'
        type: string
      environment_tag:
        description: 'Environment Tag (Format: SIT-USERID-TEAMID-YYYYMMDD-HHMM) - Must match the tag used during deployment. Example: SIT-john-teamA-20251120-1430'
        required: true
        type: string
      confirm_destroy:
        description: 'Type "destroy" to confirm'
        required: true
        type: string

env:
  TF_VERSION: '1.6.0'
  AWS_REGION: ${{ github.event.inputs.aws_region || 'eu-west-2' }}
  ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}
  ENVIRONMENT_TAG: ${{ github.event.inputs.environment_tag }}

jobs:
  setup-backend:
    name: Setup Backend Configuration
    runs-on: ubuntu-latest
    outputs:
      backend_bucket: ${{ steps.backend.outputs.backend_bucket }}
      backend_table: ${{ steps.backend.outputs.backend_table }}
    permissions:
      id-token: write
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActions-DestroyBackend
          role-duration-seconds: 28800  # 8 hours session

      - name: Derive backend configuration
        id: backend
        run: |
          # Derive backend resource names (same logic as setup script)
          AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BACKEND_BUCKET="testcontainers-terraform-state-${AWS_ACCOUNT_ID}"
          BACKEND_TABLE="testcontainers-terraform-locks"
          
          echo "backend_bucket=${BACKEND_BUCKET}" >> $GITHUB_OUTPUT
          echo "backend_table=${BACKEND_TABLE}" >> $GITHUB_OUTPUT

  validate-confirmation:
    name: Validate Destroy Confirmation
    runs-on: ubuntu-latest
    
    steps:
      - name: Check confirmation
        run: |
          if [ "${{ github.event.inputs.confirm_destroy }}" != "destroy" ]; then
            echo "::error::Destroy confirmation failed. You must type 'destroy' to proceed."
            exit 1
          fi
          echo "Destroy confirmation validated."

  terraform-destroy:
    name: Terraform Destroy
    runs-on: ubuntu-latest
    needs: [setup-backend, validate-confirmation]
    permissions:
      id-token: write
      contents: read
    environment:
      name: ${{ github.event.inputs.environment }}-destroy
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActions-TerraformDestroy
          role-duration-seconds: 28800  # 8 hours session

      - name: Create terraform.tfvars
        working-directory: AWS/terraform
        run: |
          cat > terraform.tfvars <<EOF
          aws_region        = "${{ env.AWS_REGION }}"
          project_name      = "testcontainers"
          environment       = "${{ env.ENVIRONMENT }}"
          environment_tag   = "${{ env.ENVIRONMENT_TAG }}"
          availability_zone = "${{ env.AWS_REGION }}a"
          
          vpc_cidr            = "10.0.0.0/16"
          public_subnet_cidr  = "10.0.1.0/24"
          private_subnet_cidr = "10.0.2.0/24"
          
          instance_type = "t3.medium"
          
          github_repo_url      = "https://github.com/${{ github.repository }}"
          github_runner_name   = "aws-ec2-runner-${{ env.ENVIRONMENT_TAG }}"
          github_runner_labels = ["self-hosted", "aws", "linux", "docker", "${{ env.ENVIRONMENT }}", "${{ env.ENVIRONMENT_TAG }}"]
          
          # OIDC Configuration (backend resource names derived from setup-backend job)
          github_org             = "${{ github.repository_owner }}"
          github_repo            = "${{ github.event.repository.name }}"
          terraform_state_bucket = "${{ needs.setup-backend.outputs.backend_bucket }}"
          terraform_lock_table   = "${{ needs.setup-backend.outputs.backend_table }}"
          EOF

      - name: Update backend configuration
        working-directory: AWS/terraform
        run: |
          cat > backend.tf <<EOF
          terraform {
            backend "s3" {
              bucket         = "${{ needs.setup-backend.outputs.backend_bucket }}"
              key            = "aws/ec2-runner/${{ env.ENVIRONMENT }}/${{ env.ENVIRONMENT_TAG }}/terraform.tfstate"
              region         = "${{ env.AWS_REGION }}"
              encrypt        = true
              dynamodb_table = "${{ needs.setup-backend.outputs.backend_table }}"
            }
          }
          EOF

      - name: Terraform Init
        working-directory: AWS/terraform
        run: terraform init

      - name: Terraform Plan Destroy
        working-directory: AWS/terraform
        env:
          TF_VAR_github_runner_token: "dummy-token-for-destroy"
        run: |
          # Exclude OIDC module from destroy to preserve authentication
          terraform plan -destroy \
            -target=module.ec2 \
            -target=module.security \
            -target=module.networking \
            -out=destroy-plan \
            -input=false

      - name: Terraform Destroy
        working-directory: AWS/terraform
        env:
          TF_VAR_github_runner_token: "dummy-token-for-destroy"
        run: |
          # Exclude OIDC module from destroy to preserve authentication
          terraform destroy \
            -target=module.ec2 \
            -target=module.security \
            -target=module.networking \
            -auto-approve \
            -input=false

      - name: Verify Destruction
        working-directory: AWS/terraform
        run: |
          echo "## Infrastructure Destroyed" >> $GITHUB_STEP_SUMMARY
          echo "Environment: ${{ env.ENVIRONMENT }}" >> $GITHUB_STEP_SUMMARY
          echo "Region: ${{ env.AWS_REGION }}" >> $GITHUB_STEP_SUMMARY
          echo "Timestamp: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All resources have been successfully destroyed." >> $GITHUB_STEP_SUMMARY

  cleanup-backend-option:
    name: Cleanup Backend (Optional)
    runs-on: ubuntu-latest
    needs: terraform-destroy
    if: github.event.inputs.environment == 'dev'
    permissions:
      id-token: write
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials using OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActions-BackendCleanup
          role-duration-seconds: 28800  # 8 hours session

      - name: Optional backend cleanup message
        run: |
          echo "## Backend Cleanup" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To completely remove the Terraform backend (S3 bucket and DynamoDB table)," >> $GITHUB_STEP_SUMMARY
          echo "run the cleanup script manually:" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "cd infrastructure/AWS" >> $GITHUB_STEP_SUMMARY
          echo "./scripts/cleanup-backend.sh" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ **Warning**: This will delete all Terraform state files!" >> $GITHUB_STEP_SUMMARY
